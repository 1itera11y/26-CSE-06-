#include <stdio.h>
int main() {
    char str[100];
    int k = 0;
    fgets(str,sizeof(str),stdin);
    for (int i = 0; i < 100; i++) {
        char* p = &str[i];
        if (*p != '\0') {
            k++;
        }
        else {
            printf("%d",k);
            return 0;
        }
    }
}
